from enum import Enum

class ValueErros(Enum):

    INVALID_STATES_R = 'Invalide states for R, make sur all keys of R are elements of S and A'
    INVALID_ACTIONS_R = 'Invalide actions for R, make sur all keys of R are elements of S and A'